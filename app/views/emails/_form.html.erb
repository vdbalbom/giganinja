<%= form_with(model: email, local: true) do |form| %>
  <% if email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(email.errors.count, "error") %> prohibited this email from being saved:</h2>

      <ul>
      <% email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :endereco_email %>
    <%= form.text_field :endereco_email %>
  </div>

  <div class="field">
    <%= form.label :referencia %>
    <%= form.text_field :referencia %>
  </div>

  <div class="field">
    <%= form.label :fornecedor_id %>
    <%= form.number_field :fornecedor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
